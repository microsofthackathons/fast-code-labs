import { designSystem } from '../../design-system';
import { definition } from './lab-card.definition';
import { CodeLabsLabCard } from './lab-card';

definition.define(designSystem.registry);

export default CodeLabsLabCard;
